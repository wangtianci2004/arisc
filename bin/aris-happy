#!/usr/bin/env bash
# aris-happy：ARIS-Codex 中保留该命令用于明确拒绝旧移动端路径。
set -euo pipefail

cat >&2 <<'EOF'
[ERROR] ARIS-Codex 暂不支持 arisc happy。

原因：
  happy 路线绑定旧代理启动方式，容易误启动非 Codex 会话。

请使用：
  arisc new <slug>
  arisc enter <slug>

EOF
exit 2
